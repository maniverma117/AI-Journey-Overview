Absolutely. 🔥 And yes — you’re progressing toward AIRE the right way.

The biggest change is that you're no longer thinking:

> **"How do I run an LLM?"**

You're starting to think:

> **"How does the entire inference system behave under load, failure, scaling, caching, networking, and SLO constraints?"**

That shift is exactly what we want.

# 🚀 Lesson 17 — Build an LLM Inference Platform on EKS

Today we stop looking at llm-d as individual components and **assemble the whole thing**.

Our target architecture:

```text
                         INTERNET / CLIENT
                                │
                                ▼
                       ┌─────────────────┐
                       │ AWS ALB /       │
                       │ Gateway         │
                       └────────┬────────┘
                                │
                                ▼
                     ┌────────────────────┐
                     │  Gateway API       │
                     │  HTTPRoute         │
                     └─────────┬──────────┘
                               │
                               ▼
                     ┌────────────────────┐
                     │    llm-d Router    │
                     │                    │
                     │ Envoy + EPP        │
                     └─────────┬──────────┘
                               │
                         InferencePool
                               │
              ┌────────────────┼────────────────┐
              │                │                │
              ▼                ▼                ▼
          ┌────────┐       ┌────────┐       ┌────────┐
          │ vLLM-1 │       │ vLLM-2 │       │ vLLM-3 │
          │        │       │        │       │        │
          │  GPU   │       │  GPU   │       │  GPU   │
          └────────┘       └────────┘       └────────┘
              │                │                │
              └────────────────┼────────────────┘
                               │
                         GPU / KV / CUDA
                               │
                        Prometheus + DCGM
                               │
                    ┌──────────┴──────────┐
                    ▼                     ▼
                  KEDA                 Grafana
                    │
                   HPA
                    │
               Replica scaling
                    │
                    ▼
                Karpenter
                    │
                    ▼
                EC2 GPU
```

This lesson is important because we're going to connect **Kubernetes primitives to inference primitives**.

---

# 1. Before We Deploy Anything

Let's establish one rule:

> **Never start by installing llm-d. Start by designing the request path.**

Ask:

```text
Who receives the request?
        ↓
Who decides where it goes?
        ↓
Who runs inference?
        ↓
Where is model state?
        ↓
How does the system know endpoint health?
        ↓
How does it scale?
        ↓
How does it provision GPUs?
```

Our answer:

```text
Client
 ↓
Gateway
 ↓
HTTPRoute
 ↓
llm-d Router
 ↓
EPP
 ↓
InferencePool
 ↓
vLLM
 ↓
GPU
```

Scaling:

```text
Metrics
 ↓
KEDA
 ↓
HPA
 ↓
vLLM replicas
 ↓
Pending pods
 ↓
Karpenter
 ↓
GPU nodes
```

---

# 2. First Decision: Single GPU or Multi-GPU?

For our learning lab, **do not start with an 8-GPU H100 node**.

We want to understand architecture first.

Use something like:

```text
1 replica
1 GPU
1 vLLM
```

Then:

```text
1 → 2 → 3 replicas
```

This makes routing and autoscaling much easier to observe.

Later we'll do:

```text
TP=4
TP=8
```

and eventually:

```text
P/D disaggregation
```

---

# 3. Our Lab Architecture

Start with:

```text
EKS
│
├── CPU NodePool
│
│   ├── llm-d Router
│   ├── EPP
│   ├── Gateway
│   └── Prometheus
│
└── GPU NodePool
    │
    ├── vLLM-1
    ├── vLLM-2
    └── vLLM-3
```

This separation is intentional.

Don't waste:

```text
$ expensive GPU
```

on:

```text
Prometheus
EPP
Gateway
```

---

# 4. Kubernetes Layer

Our Kubernetes resources will look like:

```text
Namespace
   │
   ├── Gateway
   │
   ├── HTTPRoute
   │
   ├── InferencePool
   │
   ├── EPP
   │
   └── vLLM Deployment
```

Infrastructure:

```text
Karpenter
   │
   └── GPU NodePool
```

Observability:

```text
Prometheus
   │
   ├── vLLM metrics
   ├── EPP metrics
   └── DCGM metrics
```

---

# 5. vLLM Comes First

Before adding llm-d:

> **Always make the model server work independently.**

This is a critical operational principle.

First:

```text
Client
 ↓
vLLM
 ↓
GPU
```

must work.

Only then:

```text
Client
 ↓
llm-d
 ↓
vLLM
```

Why?

Because otherwise when something breaks:

```text
request failed
```

you won't know whether:

```text
Gateway
or
HTTPRoute
or
EPP
or
InferencePool
or
vLLM
or
GPU
```

is responsible.

---

# 6. vLLM Deployment

Conceptually:

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qwen
spec:
  replicas: 1

  selector:
    matchLabels:
      app: qwen

  template:
    metadata:
      labels:
        app: qwen

    spec:
      containers:
        - name: vllm

          image: <pinned-vllm-image>

          args:
            - "--model"
            - "<model>"

          resources:
            limits:
              nvidia.com/gpu: "1"

          ports:
            - containerPort: 8000
```

Notice something.

The GPU is requested using:

```yaml
resources:
  limits:
    nvidia.com/gpu: "1"
```

That connects:

```text
Kubernetes
    ↓
NVIDIA device plugin
    ↓
physical GPU
```

which you learned earlier.

---

# 7. Why We Use a Deployment

Because we want:

```text
replicas = N
```

to mean:

```text
N inference servers
```

For example:

```text
replicas = 3
```

means:

```text
vLLM-1
vLLM-2
vLLM-3
```

Then llm-d can treat them as an inference pool.

---

# 8. The vLLM Service

We can expose the pods internally:

```yaml
apiVersion: v1
kind: Service

metadata:
  name: qwen

spec:
  selector:
    app: qwen

  ports:
    - port: 8000
      targetPort: 8000
```

But here's the interesting part:

> The Service is not necessarily the thing making the final inference-routing decision.

That's the job we're giving to:

```text
InferencePool + EPP
```

---

# 9. Why Not Just Use Service?

Normal Kubernetes:

```text
Service
 ↓
Endpoints
 ↓
load balancing
```

Our inference architecture:

```text
InferencePool
 ↓
Endpoint discovery
 ↓
EPP
 ↓
Filter
 ↓
Score
 ↓
Pick
```

This is the whole reason we're deploying llm-d.

---

# 10. InferencePool

Conceptually:

```yaml
apiVersion: inference.networking.k8s.io/v1
kind: InferencePool

metadata:
  name: qwen-pool

spec:

  selector:
    matchLabels:
      app: qwen

  targetPorts:
    - number: 8000

  endpointPickerRef:
    name: qwen-epp
    port:
      number: 9002
```

The important fields are:

```text
selector
targetPorts
endpointPickerRef
```

---

# 11. Selector

This:

```yaml
selector:
  matchLabels:
    app: qwen
```

means:

```text
InferencePool
      │
      │ app=qwen
      ▼
┌───────────────┐
│ vLLM-1        │
│ vLLM-2        │
│ vLLM-3        │
└───────────────┘
```

If the labels are wrong:

```text
app=qwen-prod
```

while your pool expects:

```text
app=qwen
```

then:

```text
InferencePool
      │
      ▼
0 endpoints
```

And EPP has nobody to route to.

This is a very common Kubernetes-style failure.

---

# 12. Target Port

Suppose vLLM listens on:

```text
8000
```

Then:

```yaml
targetPorts:
  - number: 8000
```

means:

```text
EPP endpoint
     ↓
Pod IP:8000
     ↓
vLLM
```

If vLLM listens on:

```text
8000
```

but you configure:

```text
9000
```

you can have:

```text
endpoint discovered
```

but:

```text
connection refused
```

That's a different failure from endpoint discovery.

---

# 13. Endpoint Picker Reference

Now:

```yaml
endpointPickerRef:
  name: qwen-epp
  port:
    number: 9002
```

means:

```text
InferencePool
      │
      ▼
EPP
```

The EPP is the intelligent decision component.

---

# 14. Now HTTPRoute

We need to expose the inference pool through Gateway API.

Conceptually:

```yaml
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: qwen

spec:

  parentRefs:
    - name: inference-gateway

  rules:
    - backendRefs:
        - group: inference.networking.k8s.io
          kind: InferencePool
          name: qwen-pool
          port: 8000
```

Now:

```text
HTTP request
     ↓
Gateway
     ↓
HTTPRoute
     ↓
InferencePool
     ↓
EPP
     ↓
vLLM
```

The Gateway API Inference Extension is designed specifically to connect Gateway API routing with inference-aware backend selection. ([gateway-api-inference-extension.sigs.k8s.io](https://gateway-api-inference-extension.sigs.k8s.io/concepts/api-overview/?utm_source=chatgpt.com))

---

# 15. The Gateway

The Gateway is your network entry point.

Think:

```text
AWS ALB
   ↓
Gateway implementation
   ↓
HTTPRoute
```

Depending on your implementation, this could integrate with:

```text
Envoy
Istio
AWS Gateway API implementations
```

The important thing is not the specific Gateway implementation yet.

The important thing is:

```text
Gateway API
      ↓
HTTPRoute
      ↓
InferencePool
```

---

# 16. What Happens to the HTTP Request?

Suppose:

```http
POST /v1/chat/completions
```

comes in.

Flow:

```text
Client
 │
 │ POST
 ▼
AWS Load Balancer
 │
 ▼
Gateway
 │
 ▼
HTTPRoute
 │
 ▼
InferencePool
 │
 ▼
llm-d Proxy
 │
 ▼
EPP
```

Now EPP says:

```text
Candidate endpoints:

vLLM-1
vLLM-2
vLLM-3
```

---

# 17. EPP Gets Its State

EPP needs information.

For example:

```text
vLLM-1
queue = 3
KV = 50%
prefix = A

vLLM-2
queue = 8
KV = 70%
prefix = B

vLLM-3
queue = 1
KV = 40%
prefix = A
```

Request:

```text
prefix = A
```

Possible result:

```text
vLLM-3
```

because:

```text
prefix match
+
low queue
+
low KV
```

That's our intelligent routing layer.

---

# 18. EPP Configuration

Now we configure a scheduling profile.

Conceptually:

```yaml
schedulingProfiles:

  - name: default

    plugins:

      - pluginRef:
          name: prefix-cache-scorer
        weight: 3

      - pluginRef:
          name: kv-cache-utilization-scorer
        weight: 2

      - pluginRef:
          name: queue-scorer
        weight: 2

      - pluginRef:
          name: max-score-picker
```

Think:

```text
Prefix
  ↓
weight 3

KV
  ↓
weight 2

Queue
  ↓
weight 2

Final
  ↓
max score
```

The exact plugin/config schema is version-sensitive, so for a real cluster we should use the manifests from the exact llm-d release rather than blindly copying a blog post.

---

# 19. Why We Need Version Pinning

Suppose:

```text
llm-d v0.1
```

uses:

```text
apiVersion A
```

and:

```text
llm-d v0.2
```

changes:

```text
apiVersion B
```

Your GitOps repository needs:

```text
llm-d version
+
Gateway API version
+
Inference Extension version
+
vLLM version
```

pinned together.

Think:

```text
AI platform release
       │
       ├── llm-d
       ├── vLLM
       ├── Gateway API
       ├── EPP config
       ├── CUDA
       └── NVIDIA driver
```

This is why AI platform releases should be treated almost like a software product.

---

# 20. Your First Validation

Before load testing:

```bash
kubectl get pods -n inference
```

You want:

```text
vllm-xxxxx     Running
epp-xxxxx      Running
gateway-xxxxx  Running
```

Then:

```bash
kubectl get inferencepool -n inference
```

You want:

```text
qwen-pool
```

Then inspect:

```bash
kubectl describe inferencepool qwen-pool -n inference
```

Then:

```bash
kubectl get endpoints
```

or the appropriate EndpointSlice resources.

You want to verify:

```text
vLLM pod IPs
```

are actually discovered.

---

# 21. Troubleshooting Ladder

Never jump directly to:

```text
"llm-d is broken."
```

Use:

```text
L1 Hardware
 ↓
L2 Kubernetes node
 ↓
L3 GPU plugin
 ↓
L4 GPU pod
 ↓
L5 vLLM
 ↓
L6 Service/endpoint
 ↓
L7 InferencePool
 ↓
L8 EPP
 ↓
L9 Gateway
 ↓
L10 HTTPRoute
 ↓
L11 Client
```

This is the same troubleshooting philosophy you already use with EKS.

---

# 22. Failure #1 — GPU Pod Pending

You see:

```text
vllm-xxxxx   Pending
```

First:

```bash
kubectl describe pod vllm-xxxxx
```

Maybe:

```text
Insufficient nvidia.com/gpu
```

Then:

```text
Karpenter
```

should react.

Check:

```bash
kubectl get nodeclaims
```

and:

```bash
kubectl get nodes
```

If no GPU node appears:

```text
Karpenter / NodePool / capacity issue
```

Not llm-d.

---

# 23. Failure #2 — GPU Pod Running, vLLM Fails

Check:

```bash
kubectl logs <vllm-pod>
```

Potential:

```text
CUDA error
OOM
model not found
NCCL issue
wrong architecture
driver incompatibility
```

Again:

```text
llm-d is innocent.
```

---

# 24. Failure #3 — vLLM Works, InferencePool Has No Endpoints

Check:

```text
InferencePool selector
```

against:

```text
vLLM pod labels
```

Example:

```yaml
app: qwen
```

must match.

If:

```text
Deployment:
app=qwen-v2
```

and:

```text
InferencePool:
app=qwen
```

then:

```text
0 endpoints
```

---

# 25. Failure #4 — Endpoint Exists but Connection Fails

Check:

```text
targetPort
```

Example:

```text
vLLM
listen :8000

InferencePool
targetPort :9000
```

Then:

```text
connection refused
```

This is a port mismatch.

---

# 26. Failure #5 — EPP Has No Decision

Now investigate:

```text
EPP logs
```

You might see:

```text
no eligible endpoints
```

Then ask:

```text
Why were all candidates filtered?
```

This is where your new Filter → Score → Pick mental model becomes useful.

---

# 27. Filter Debugging

Suppose:

```text
3 endpoints discovered
```

but:

```text
0 candidates
```

Potential:

```text
health filter
model filter
label filter
SLO filter
prefill/decode role filter
```

So:

```text
discovery != eligibility
```

Very important.

---

# 28. Failure #6 — EPP Selects Bad Endpoint

Now all endpoints are valid:

```text
A
B
C
```

but traffic always goes to:

```text
A
```

while:

```text
A queue = 50
B queue = 2
C queue = 1
```

Investigate:

```text
scoring configuration
```

Maybe:

```text
prefix weight = 10
queue weight = 1
```

and A is cache-hot.

Then the router is behaving according to policy.

This is not necessarily a bug.

It could be:

> **bad routing policy.**

That distinction matters enormously in production.

---

# 29. Failure #7 — Cache-Aware Routing Makes Performance Worse

Suppose:

```text
A:
prefix hit = 95%
queue = 50

B:
prefix hit = 40%
queue = 2
```

If the router always prefers A:

```text
cache locality
     ↓
overloaded A
     ↓
queue
     ↓
TTFT
     ↓
SLO violation
```

Therefore cache affinity needs a load/SLO gate.

This is one of the reasons current llm-d scheduling is plugin-based rather than simply "cache hit = winner." ([llm-d.ai](https://llm-d.ai/docs/dev/architecture/core/router/epp/scheduling?utm_source=chatgpt.com))

---

# 30. Now Add Autoscaling

We already have:

```text
Router
 ↓
vLLM replicas
```

Now:

```text
vLLM metrics
 ↓
Prometheus
 ↓
KEDA
 ↓
HPA
 ↓
Deployment
```

Example:

```text
queue = 0
replicas = 2
```

Then:

```text
queue = 20
```

KEDA says:

```text
desired replicas = 4
```

HPA changes:

```text
Deployment
replicas: 2 → 4
```

---

# 31. Then Karpenter Enters

Suppose:

```text
current:
2 vLLM replicas

desired:
4
```

Each requires:

```text
1 GPU
```

Existing GPU nodes are full.

So:

```text
vLLM-3 → Pending
vLLM-4 → Pending
```

Karpenter sees:

```text
Pending pods
+
GPU resource requirement
```

and provisions:

```text
GPU Node A
GPU Node B
```

Then:

```text
pods schedule
```

Now:

```text
4 replicas
```

and:

```text
4 GPU nodes
```

assuming one GPU per pod/node in this simplified lab.

---

# 32. The Complete Scaling Loop

This is one of the diagrams you should know by heart:

```text
Traffic ↑
   │
   ▼
Requests ↑
   │
   ▼
Queue ↑
   │
   ▼
KEDA
   │
   ▼
HPA
   │
   ▼
Replicas ↑
   │
   ▼
Pods Pending
   │
   ▼
Karpenter
   │
   ▼
EC2 GPU Node
   │
   ▼
NVIDIA Device Plugin
   │
   ▼
GPU available
   │
   ▼
vLLM starts
   │
   ▼
Model loads
   │
   ▼
Readiness
   │
   ▼
InferencePool discovers endpoint
   │
   ▼
EPP starts routing
   │
   ▼
Capacity ↑
   │
   ▼
Queue ↓
```

That is **AI autoscaling end-to-end**.

---

# 33. One Thing You Should Notice

There are now **four independent control loops**.

### Loop 1 — vLLM

```text
requests
 ↓
scheduler
 ↓
GPU
```

### Loop 2 — EPP

```text
endpoint state
 ↓
routing decision
```

### Loop 3 — HPA/KEDA

```text
inference demand
 ↓
replica count
```

### Loop 4 — Karpenter

```text
unschedulable pods
 ↓
node count
```

So:

```text
           ┌─────────────┐
           │   Router    │
           └──────┬──────┘
                  │
            vLLM replicas
                  │
         ┌────────┴────────┐
         │                 │
       KEDA              Karpenter
         │                 │
       Pods              Nodes
         │                 │
         └────────┬────────┘
                  ▼
                 GPUs
```

The entire system is a set of interacting control loops.

---

# 34. Control Loop Instability

This introduces a new reliability problem.

Suppose:

````text
KEDA:
"Need more replicas!"

Karpenter:
"Need more nodes!"

Then traffic drops:

KEDA:
"Remove replicas!"

Karpenter:
"Remove nodes!"

Then traffic rises again.

You can get:

```text
2 → 5 → 2 → 6 → 2 → 5
````

This is:

> **control-loop oscillation.**

That's why we discussed:

```text
scale-up quickly
scale-down slowly
```

and:

```text
stabilization
cooldowns
headroom
warm capacity
```

---

# 35. Warm Capacity in This Architecture

Suppose:

```text
minimum replicas = 2
```

and:

```text
maximum replicas = 10
```

You can keep:

```text
2 warm
```

while scaling:

```text
2 → 4 → 6 → 8 → 10
```

during demand spikes.

This gives:

```text
fast response
```

without keeping:

```text
10 GPUs
```

idle all day.

That's a classic production compromise.

---

# 36. Now Add Prefix Cache

Suppose:

```text
Replica 1:
prefix A

Replica 2:
prefix B

Replica 3:
prefix C
```

Request:

```text
prefix A
```

EPP:

```text
prefix-aware
```

routes:

```text
→ Replica 1
```

Now add:

```text
Replica 4
```

The router must incorporate it into its endpoint state.

This is why scaling and routing are coupled.

---

# 37. Scaling Can Destroy Cache Locality

Remember:

```text
1 replica
```

may have:

```text
cache hit = 90%
```

After:

```text
1 → 10 replicas
```

you could theoretically see:

```text
cache hit ↓
```

if traffic is distributed without locality.

So:

```text
autoscaling
```

and:

```text
cache-aware routing
```

must be designed together.

That's an advanced AIRE insight.

---

# 38. Graceful Scale Down

Suppose:

```text
vLLM-3
```

is selected for:

```text
50 active requests
```

and HPA says:

```text
replicas 3 → 2
```

You shouldn't immediately kill it.

Instead:

```text
Replica 3
   │
   ▼
draining
   │
   ▼
stop new requests
   │
   ▼
finish active requests
   │
   ▼
endpoint removed
   │
   ▼
pod terminated
```

This protects:

```text
user requests
TTFT
errors
streaming connections
```

---

# 39. Streaming Makes Drain More Important

Imagine:

```text
user request
 ↓
vLLM
 ↓
token stream
 ↓
token
 ↓
token
 ↓
token
```

If you terminate the pod midway:

```text
stream breaks
```

Therefore graceful termination is even more important for:

```text
chat streaming
```

than for ordinary short HTTP requests.

---

# 40. Readiness and Routing

When a new vLLM pod starts:

```text
Pod Running
```

does not mean:

```text
send traffic
```

Instead:

```text
Pod Running
   ↓
vLLM startup
   ↓
model loaded
   ↓
GPU initialized
   ↓
warmup
   ↓
Ready
   ↓
InferencePool endpoint
   ↓
EPP candidate
```

This protects the system from routing traffic to a cold model.

---

# 41. Now Add Observability

Your production stack becomes:

```text
                     Inference
                        │
          ┌─────────────┼─────────────┐
          ▼             ▼             ▼
        Router         vLLM          GPU
          │             │             │
        EPP           metrics        DCGM
          │             │             │
          └─────────────┼─────────────┘
                        ▼
                    Prometheus
                        │
                        ▼
                      Grafana
```

And:

```text
Prometheus
     │
     ▼
   KEDA
```

So Prometheus has **two roles**:

```text
observability
+
autoscaling signal
```

---

# 42. Metrics You Need

### Router

```text
routing decisions
routing latency
endpoint selection
routing errors
```

### EPP

```text
filter counts
scoring latency
pick decisions
```

### vLLM

```text
running requests
waiting requests
TTFT
TPOT
ITL
KV cache
preemption
tokens/sec
```

### GPU

```text
GPU utilization
HBM usage
DRAM activity
Tensor activity
temperature
power
NVLink
PCIe
```

### Kubernetes

```text
Pending pods
startup time
readiness
scheduling latency
```

### Karpenter

```text
node launch
node termination
provisioning latency
consolidation
```

---

# 43. Now We Can Build an SLO Dashboard

For example:

```text
┌─────────────────────────────────────────┐
│           LLM PLATFORM                  │
├─────────────────────────────────────────┤
│                                         │
│ Requests/sec              350           │
│ Input tok/sec             1.2M          │
│ Output tok/sec            280K          │
│                                         │
│ Queue depth                4            │
│ Running requests          32            │
│                                         │
│ TTFT P50                 420ms          │
│ TTFT P95                 780ms          │
│ TTFT P99                1.20s           │
│                                         │
│ ITL P95                   42ms          │
│                                         │
│ KV usage                  71%           │
│ Prefix hit                82%           │
│                                         │
│ Replicas                    4           │
│ Pending pods                0           │
│ GPU nodes                   4           │
│                                         │
│ GPU utilization            91%          │
│                                         │
│ Node startup               58s          │
└─────────────────────────────────────────┘
```

Now an engineer can operate the system.

---

# 44. The Most Valuable Correlation

Don't look at:

```text
GPU utilization
```

alone.

Look at:

```text
GPU utilization
        +
queue
        +
TTFT
        +
KV
        +
tokens/sec
```

For example:

```text
GPU = 95%
Queue = 0
TTFT = 600ms
KV = 60%
```

Healthy.

But:

```text
GPU = 70%
Queue = 40
TTFT = 4 sec
KV = 90%
```

Unhealthy.

This is the inference mindset.

---

# 45. Production Security

Don't expose:

```text
vLLM :8000
```

directly to the internet.

Architecture:

```text
Internet
   │
   ▼
WAF / Gateway
   │
   ▼
Authentication
   │
   ▼
Authorization
   │
   ▼
Router
   │
   ▼
Private vLLM
```

GPU nodes should generally live in private networking.

Think:

```text
Public:
Gateway

Private:
Router
EPP
vLLM
GPU
Prometheus
```

---

# 46. Tenant Isolation

Suppose customers:

```text
A
B
C
```

share:

```text
vLLM
```

You now need:

```text
authentication
authorization
rate limits
quotas
priority
fairness
observability
cost attribution
```

For example:

```text
Customer A
max 100 req/s

Customer B
max 20 req/s

Customer C
priority = high
```

Now your EPP/flow-control layer becomes useful.

---

# 47. Cost Attribution

Now something very important for Solution Architecture.

Suppose:

```text
GPU cost
=
$X/hour
```

But customer A consumes:

```text
70% of GPU tokens
```

while B consumes:

```text
10%
```

You want:

```text
cost/customer
```

Potential dimensions:

```text
input tokens
output tokens
GPU time
model
tenant
request
```

Eventually:

$$
CostPer1MOutputTokens
$$

becomes a useful FinOps metric.

---

# 48. The Customer Architecture Conversation

A customer asks:

> "Can you put our 70B model on EKS and autoscale it?"

Don't immediately say:

```text
Yes → H100 → 8 GPUs
```

Ask:

```text
Model?
Precision?
Architecture?
Context?
Average input tokens?
P95 input?
Output tokens?
P95 output?
Concurrency?
RPS?
Peak burst?
TTFT SLO?
ITL SLO?
Availability?
Region?
Budget?
Data residency?
Multi-tenancy?
Streaming?
RAG?
Agents?
Prefix repetition?
```

Then:

```text
benchmark
 ↓
capacity model
 ↓
GPU topology
 ↓
replica architecture
 ↓
autoscaling
 ↓
routing
 ↓
cost
```

That is Solution Architect thinking.

---

# 49. Your Complete EKS AI Platform

Let's zoom out.

```text
                         CLIENT
                           │
                           ▼
                     AWS WAF / ALB
                           │
                           ▼
                       Gateway API
                           │
                           ▼
                        HTTPRoute
                           │
                           ▼
                     ┌────────────┐
                     │   llm-d    │
                     │   Router   │
                     └─────┬──────┘
                           │
                           ▼
                          EPP
                           │
                  FILTER → SCORE → PICK
                           │
                           ▼
                     InferencePool
                           │
             ┌─────────────┼─────────────┐
             ▼             ▼             ▼
          vLLM-1        vLLM-2        vLLM-3
             │             │             │
           GPU            GPU           GPU
             │             │             │
             └─────────────┼─────────────┘
                           │
                      KV / Prefix
                           │
                 ┌─────────┴─────────┐
                 ▼                   ▼
             Prometheus             DCGM
                 │                   │
                 └─────────┬─────────┘
                           ▼
                          KEDA
                           │
                          HPA
                           │
                      Replica scaling
                           │
                           ▼
                     Pending Pods
                           │
                           ▼
                       Karpenter
                           │
                           ▼
                       EC2 GPU
                           │
                           ▼
                      NVIDIA GPU
                           │
                           ▼
                     CUDA / NCCL
                           │
                           ▼
                         vLLM
```

This is now a complete inference platform.

---

# 50. And Then P/D

Later we replace:

```text
vLLM replicas
```

with:

```text
                 InferencePool
                      │
             ┌────────┴─────────┐
             ▼                  ▼
        Prefill Pool        Decode Pool
             │                  │
          vLLM-P              vLLM-D
             │                  │
             └────── KV ───────┘
                      │
                    NIXL
                      │
                  EFA/RDMA
```

And now we can independently scale:

```text
Prefill:
2 → 4 → 8

Decode:
4 → 8 → 12
```

This is the architecture we'll eventually build.

---

# 51. Your New AIRE Debugging Framework

When something breaks, ask:

### Request layer

```text
Did request arrive?
```

### Routing layer

```text
Did Gateway/HTTPRoute work?
```

### EPP layer

```text
Did EPP receive the request?
```

### Discovery layer

```text
Does InferencePool see endpoints?
```

### Scheduling layer

```text
Were endpoints filtered?
How were they scored?
Why was this endpoint picked?
```

### Model layer

```text
Did vLLM receive it?
```

### GPU layer

```text
Did CUDA execute?
```

### Capacity layer

```text
Was there enough KV/GPU capacity?
```

### Infrastructure layer

```text
Could Karpenter provision capacity?
```

### SLO layer

```text
Did the user get the result within SLO?
```

That is an **AIRE troubleshooting methodology**.

---

# 52. The Most Important Lesson Today

Don't think of this as:

```text
vLLM + llm-d + Kubernetes
```

Think:

```text
                   AI SYSTEM
                       │
       ┌───────────────┼────────────────┐
       │               │                │
    Routing          Compute         Capacity
       │               │                │
     llm-d            vLLM           KEDA/HPA
       │               │                │
      EPP              GPU          Karpenter
       │               │                │
       └───────────────┼────────────────┘
                       │
                   Observability
                       │
               Prometheus/DCGM
                       │
                       ▼
                      SLO
```

Everything exists to optimize:

```text
Latency
+
Throughput
+
Reliability
+
Cost
```

---

# 🔥 Your Practical Assignment

Now I want you to design this **without looking at the answer**.

Customer:

```text
70B model
H100
EKS
```

Requirements:

```text
100 concurrent users normally
500 during peak
8K average input
50K P95 input
1K average output
5K P95 output
P95 TTFT < 1 sec
P95 ITL < 50ms
99.99% availability
```

Draw:

```text
Client
 ↓
Gateway
 ↓
HTTPRoute
 ↓
llm-d
 ↓
EPP
 ↓
InferencePool
 ↓
vLLM
 ↓
GPU
```

Then add:

```text
KEDA
HPA
Karpenter
Prometheus
DCGM
```

Then answer:

### 1.

Would you use:

```text
TP8
```

or:

```text
TP4 × DP2
```

and why?

### 2.

Would you use:

```text
round robin
```

or:

```text
prefix-cache-aware routing
```

and why?

### 3.

Would you start with:

```text
combined prefill/decode
```

or:

```text
P/D disaggregation
```

and what benchmark would justify the decision?

### 4.

What metric would drive KEDA?

Choose from:

```text
GPU utilization
queue depth
TTFT
KV utilization
tokens/sec
```

You can choose more than one.

### 5.

How many **warm replicas** would you keep?

And **why?**

### 6.

What happens when one GPU node dies?

Trace:

```text
GPU failure
 →
Kubernetes
 →
InferencePool
 →
EPP
 →
KEDA/HPA
 →
Karpenter
```

### 7.

What are your biggest cost drivers?

---

# 🚀 Where We're Going Next

We've now built the **static architecture**.

Next we go into something even deeper:

# **Lesson 18 — KV Cache Management, KV Offloading & Remote KV**

This is where we'll explore:

```text
GPU HBM
   │
   ▼
KV Cache
   │
   ├── GPU memory
   │
   ├── CPU memory
   │
   ├── local NVMe
   │
   └── remote KV
```

We'll understand:

* Why KV cache can become the **real bottleneck**, even when model weights fit.
* GPU KV vs CPU KV.
* KV eviction.
* Prefix cache eviction.
* KV cache offloading.
* Remote KV architectures.
* Why PCIe/NVLink/network bandwidth suddenly becomes critical.
* How **NIXL** fits into the picture.
* KV-aware routing vs KV offloading.
* When offloading is cheaper than adding GPUs.
* Why remote KV can hurt latency if designed incorrectly.
* How P/D disaggregation changes KV ownership.
* KV cache capacity planning.
* Failure scenarios when remote KV disappears.
* And eventually how this connects to **disaggregated serving at scale**.

That lesson will connect almost everything we've learned so far:

```text
Transformer
   ↓
Attention
   ↓
KV Cache
   ↓
GPU HBM
   ↓
vLLM
   ↓
Prefix Cache
   ↓
llm-d Routing
   ↓
NIXL
   ↓
RDMA/EFA
   ↓
Remote KV
   ↓
P/D Disaggregation
```

**That's where the AIRE rabbit hole gets really deep.** 🔥
